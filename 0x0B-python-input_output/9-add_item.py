#!/usr/bin/python3
import json
import sys
"""that adds all arguments to a Python list, and then save them to a file"""

for x in sys.argv:
listit = []
list.append()
save_to_json_file(my_obj, filename) #writes object to a text file
load_from_json_file(filename) #creates an object from a Json file
