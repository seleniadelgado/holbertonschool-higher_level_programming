sort a list
>>> MyList = __import__('1-my_list').MyList
    >>> my_list = MyList()
    >>> print(my_list)
    []
    >>> my_list = MyList()
    >>> my_list.append(2)
    >>> my_list.append(1)
    >>> my_list.append(0)
    >>> print(my_list)
    [2, 1, 0]
    >>> my_list = MyList()
    >>> my_list.append(2)
    >>> my_list.append(1)
    >>> my_list.append(0)
    >>> my_list.print_sorted()
    [0, 1, 2]
    >>> my_list = MyList()
    >>> my_list.append(-2)
    >>> my_list.append(-1)
    >>> my_list.append(0)
    >>> my_list.print_sorted()
    [-2, -1, 0]
    >>> my_list.append(float("inf"))
    >>> my_list.print_sorted()
    [-2, -1, 0, inf]
    >>> my_list.append("nan")
